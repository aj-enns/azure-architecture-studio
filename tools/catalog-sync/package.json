{
  "name": "@aas/catalog-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Detects new Azure resource types (AVM + official icon) and scaffolds catalog draft candidates.",
  "scripts": {
    "sync": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aas/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}

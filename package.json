{
  "name": "azure-architecture-review",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source, self-hostable Azure architecture diagram builder for ARB processes.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "diagram:solution": "pnpm --filter @aar/shared build && node tools/generate-solution-diagram.mjs",
    "catalog:sync": "pnpm --filter @aar/catalog-sync sync"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/node": "^22.10.5",
    "eslint": "^9.36.0",
    "globals": "^16.4.0",
    "prettier": "^3.4.2",
    "typescript-eslint": "^8.45.0",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "overrides": {
      "fast-uri@3.1.4": "3.1.6",
      "fast-uri@4.1.1": "4.1.3"
    }
  }
}
